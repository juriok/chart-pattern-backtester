services:
  pattern-bot:
    build: .
    container_name: pattern-bot
    restart: unless-stopped
    environment:
      - TZ=UTC
      # Optional Telegram alerts — set in .env or here:
      # - TELEGRAM_BOT_TOKEN=...
      # - TELEGRAM_CHAT_ID=...
    volumes:
      - ./state:/app/state
